/**
 * says hello to the user or anthing else
 * @param {string} somebody - the name of the person or nickname or surname or...
 * 
 * @returns {void} - nothing, its a procedure
 * 
 * @author wuja
 */

const sayHello = (somebody) => {
    alert(`Hello ${somebody}`);
};